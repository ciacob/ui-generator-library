# ui-generator-library
Library that uses AS3 reflection to build a basic UI of components that bind back to reflected class' respective public fields. A number of supported custom annotations make the resulting UI more flexible (note that the compiler must make aware that it must retain those custom annotations).
